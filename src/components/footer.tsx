import { FooterResource } from "./blocks/footer-resource";

export const Footer = () => {
  return (
    <footer className="flex flex-col justify-center items-center bg-muted">
      <FooterResource />
    </footer>
  );
};
